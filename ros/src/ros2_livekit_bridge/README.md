# ros2_livekit_bridge
An out of the box ROS2 node to bridge the ROS2 graph and the livekit ecosystem

# Setup
Ensure you are able to run the examples of the bridge [README.md](bridge/README.md)


# Config
The config is stored in the `config/ros2_livekit_bridge_params.yaml` file.
